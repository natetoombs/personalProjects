/*
    #pragma once
    has the same fuction as the header guard. It is not recognized by all
    compilers, but really, it probably is; use it.
*/

#ifndef ADD_H
#define ADD_H

int add(int x, int y); // function prototype for add.h -- don't forget the semicolon!

#endif
